local redzlib = loadstring(game:HttpGet("https://raw.githubusercontent.com/newredz/RedzLibV5/refs/heads/main/Source.lua"))()

local Window = redzlib:MakeWindow({"Unnamed HUB", "By John", "testando-redzLibv5.json"})
Window:AddMinimizeButton({
  Button = { Image = redzlib:GetIcon("rbxassetid://18751483361"), BackgroundTransparency = 0 },
  Corner = { CornerRadius = UDim.new(0, 8) }
})

-- Tabs
local Tab1 = Window:MakeTab({"Main", "Home"})
local Tab2 = Window:MakeTab({"Player", "User"})
local Tab3 = Window:MakeTab({"Battle", "Sword"})
local Tab4 = Window:MakeTab({"Class & Train", "ShoppingCart"})
local Tab5 = Window:MakeTab({"Teleport", "locate"})
local Tab6 = Window:MakeTab({"Visual", "eye"})
local Tab7 = Window:MakeTab({"Script", "List"})
local Tab8 = Window:MakeTab({"Useless", "Mail"})
local Tab9 = Window:MakeTab({"Misc", "Setting"})

Tab1:AddSection({"Main Feature of The Script"})
Tab1:AddParagraph({
    Name = "Main Paragraph",
    Text = "Main Tab for something idk HAHA"
})

local teleporting = false
local teleportDelay = 0.2

local Dropdown = Tab1:AddDropdown({
    Name = "Teleport Delay",
    Description = "Select the delay between teleports",
    Options = {"0.1", "0.3", "1"},
    Default = "0.2",
    Flag = "teleport_delay",
    Callback = function(Value)
        teleportDelay = tonumber(Value)
    end
})

Tab1:AddToggle({
    Name = "Spam Teleport to Conductor Seat",
    Default = false,
    Callback = function(state)
        teleporting = state
        while teleporting do
            local rootPart = game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if rootPart then
                local rootPart = game.Players.LocalPlayer.Character.HumanoidRootPart

                -- New teleportation logic with notification
                local StarterGui = game:GetService("StarterGui")

                local function notify(trainName)
                    StarterGui:SetCore("SendNotification", {
                        Title = "[Unnamed HUB] Teleport",
                        Text = "Teleported to " .. trainName,
                        Duration = 2,
                        Icon = "rbxthumb://type=Asset&id=5107182114&w=150&h=150"
                    })
                end

                if workspace:FindFirstChild("armor") then
                    rootPart.CFrame = workspace.armor.RequiredComponents.Controls.ConductorSeat.VehicleSeat.CFrame
                    notify("Armored Train")
                elseif workspace:FindFirstChild("golden") then
                    rootPart.CFrame = workspace.golden.RequiredComponents.Controls.ConductorSeat.VehicleSeat.CFrame
                    notify("Golden Train")
                elseif workspace:FindFirstChild("default") then
                    rootPart.CFrame = workspace.default.RequiredComponents.Controls.ConductorSeat.VehicleSeat.CFrame
                    notify("Default Train")
                elseif workspace:FindFirstChild("cattle") then
                    rootPart.CFrame = workspace.cattle.RequiredComponents.Controls.ConductorSeat.VehicleSeat.CFrame
                    notify("Cattle Train")
                end
            end
            task.wait(teleportDelay)
        end
    end
})

local runtimeItems = workspace:WaitForChild("RuntimeItems")
local replicatedStorage = game:GetService("ReplicatedStorage")
local storeItemRemote = replicatedStorage:WaitForChild("Remotes"):WaitForChild("StoreItem")
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()

local Run = false

Tab1:AddToggle({
	Name = "Auto pick up Item",
	Default = false,
	Callback = function(state)
		Run = state
	end
})

task.spawn(function()
	while true do
		if Run then
			for _, model in ipairs(runtimeItems:GetChildren()) do
				if model:IsA("Model") then
					local tooFar = false
					for _, part in ipairs(model:GetDescendants()) do
						if part:IsA("Part") or part:IsA("MeshPart") or part:IsA("UnionOperation") then
							if character.PrimaryPart then
								local distance = (part.Position - character.PrimaryPart.Position).Magnitude
								if distance > 10 then
									tooFar = true
									break
								end
							end
						end
					end

					if tooFar then
						if not model:FindFirstChildOfClass("Highlight") then
							local highlight = Instance.new("Highlight")
							highlight.Parent = model
							highlight.FillTransparency = 0.5
							highlight.FillColor = Color3.new(1, 1, 1)
							highlight.OutlineTransparency = 1
						end
					else
						storeItemRemote:FireServer(model)
					end
				end
			end
		end
		task.wait(0.01)
	end
end)

Tab1:AddButton({
    Name = "Teleport to Conductor Seat",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/YourRealjohn/Dead-Trails-Lua/refs/heads/main/Train%20TP", true))()
    end
})

Tab1:AddSection({"Weld object in the air Basically for trolling"})

Tab1:AddButton({
    Name = "Air Weld Updated",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/YourRealjohn/Dead-Trails-Lua/refs/heads/main/Air%20Weld%20Unnamed%20HUB"))()
    end
})

Tab1:AddButton({"Enable Fly Anti-Cheat Bypass", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/m00ndiety/FLY-anticheat-bypass/refs/heads/main/AntiCheatBypassMoon.txt"))()
end})

Tab1:AddButton({"Teleport to Unicorn", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Shady123h5/XvshadyTp/refs/heads/main/TptoUnicorn.lua"))()
end})


Tab2:AddSection({"Noclip known as Wall hack in happy mod"})

local Noclip = nil
local Clip = true

local function noclip()
    Clip = false
    Noclip = game:GetService("RunService").Stepped:Connect(function()
        local character = game.Players.LocalPlayer.Character
        if character and not Clip then
            for _, v in pairs(character:GetDescendants()) do
                if v:IsA("BasePart") then
                    v.CanCollide = false
                end
            end
        end
    end)
end

local function clip()
    Clip = true
    if Noclip then
        Noclip:Disconnect()
        Noclip = nil
    end
    local character = game.Players.LocalPlayer.Character
    if character then
        for _, v in pairs(character:GetDescendants()) do
            if v:IsA("BasePart") then
                v.CanCollide = true
            end
        end
    end
end

Tab2:AddToggle({
    Name = "Enable Noclip",
    Default = false,
    Callback = function(state)
        if state then
            noclip()
        else
            clip()
        end
    end
})

Tab5:AddSection({Teleport tab"})
Tab5:AddParagraph({
    Name = " Paragraph",
    Text = "Tp Lua by Moondiety"
})


Tab5:AddButton({"Teleport to Lab", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/m00ndiety/tp-to-lab/refs/heads/main/obf_bgVIHW4PEtb5Bh89XaBFg9S72Y4jwTuIcPLsMprwm4W9eSI6f9W4JBXW39e2S3Mr.lua.txt"))()
end})

Tab5:AddButton({"Teleport to Sterling", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/Shady123h5/XvshadyTp/refs/heads/main/XvShadyTpToSterlingv1.txt"))()
end})

Tab5:AddButton({"Teleport to Fort Constitution", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/m00ndiety/tp-to-fort-constitution-/refs/heads/main/obf_KGsB4mk27SGbD6M8aszkIee8s6a9HTD6A1460637gyJ0aT3ai6q3EX9eiNEk91xx.lua.txt"))()
end})

Tab5:AddButton({"Teleport to Castle", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/m00ndiety/tp-castle-/refs/heads/main/obf_f8CdDAgMTI8UYXfim488VbOUt079Ktng2zXx84l9cm26hIMuZ3kYNN95Ymb3lSuB.lua.txt"))()
end})

Tab5:AddButton({"Teleport to End", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/m00ndiety/tp-to-end-final1/refs/heads/main/obf_tp_to_end_final_1.lua.txt"))()
end})

Tab6:AddSection({"For Visual"})
Tab6:AddParagraph({
    Name = "Paragraph",
    Text = "Visual tab for ESP Object unlocking ThirdPerson Camera Shaders and Animation"
})

Tab6:AddSection({"ESP"})

-- ESP ITEMS AND MOBS
loadstring(game:HttpGet("https://raw.githubusercontent.com/YourRealjohn/Unnamed-HUB-Lua/refs/heads/main/ESP%20Items%20and%20Mobs%20DEAD%20RAILS"))()

Tab6:AddToggle({
    Name = "ESP Items and Mobs [fixing]",
    CurrentValue = false,
    Flag = "ESPAllToggle",
    Callback = function(Value)
        ESPEnabled = Value
        if Value then
            UpdateESP()
            coroutine.wrap(AutoUpdateESP)()
        else
            ClearESP()
        end
    end
})

Tab6:AddToggle({
    Name = "ESP Unicorn [Coming soon]",
    CurrentValue = false,
    Flag = "ESPAllToggle",
    Callback = function(Value)
        ESPEnabled = Value
        if Value then
            UpdateESP()
            coroutine.wrap(AutoUpdateESP)()
        else
            ClearESP()
        end
    end
})

-- Load ESP script
loadstring(game:HttpGet("https://raw.githubusercontent.com/YourRealjohn/Unnamed-HUB-Lua/refs/heads/main/ESP%20PLAYER%20WITH%20NOTIFICATION%20UNNAMED%20HUB"))()

-- Add ESP toggle
Tab6:AddToggle({
    Name = "Infinite yield ESP Player",
    CurrentValue = false,
    Flag = "ESPAllToggle",
    Callback = function(state)
        toggleESP(state)
    end
})


Tab6:AddToggle({
	Name = "Unlock ThirdPerson",
	Default = false,
	Callback = function(state)
		local player = game:GetService("Players").LocalPlayer
		if state then
			player.CameraMaxZoomDistance = 280
			player.CameraMode = Enum.CameraMode.Classic
		else
			player.CameraMaxZoomDistance = 10
			player.CameraMode = Enum.CameraMode.LockFirstPerson
		end
	end
})

Tab7:AddSection({"Script Tab"})
Tab7:AddParagraph({
    Name = "Paragraph",
    Text = "Some random Useful for making script and Give you advantage"
})

Tab7:AddButton({
    Name = "Vfly Script Using Any Seat",
    Callback = function() loadstring(game:HttpGet('https://raw.githubusercontent.com/GhostPlayer352/Test4/main/Vehicle%20Fly%20Gui'))()
    end
})

Tab9:AddSection({"Setting"})
local Paragraph = Tab9:AddParagraph({
    Name = "Settings Paragraph",
    Text = "Setting tab this is where you can Server hop or Rejoin or Join to other server using Job id and others"
})

Tab9:AddSection({"Join to another Server using Job id"})
Tab9:AddTextBox({
    Name = "Job ID",
    Default = "",
    TextDisappear = true,
    Callback = function(v)
	    _G.Job = v
    end
})

Tab9:AddButton({"Join Server", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/YourRealjohn/Unnamed-HUB-Lua/refs/heads/main/Teleport%20tp%20the%20job%20id%20Saved"))()
end})

Tab9:AddButton({"COPY THE SERVER ID", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/YourRealjohn/Unnamed-HUB-Lua/refs/heads/main/Copy%20Server%20Job%20ID"))()
end})

Tab9:AddSection({"Teleport to Lobby"})

Tab9:AddButton({
    Name = "Teleport to Lobby",
    Callback = function()
        Window:Dialog({
            Title = "Confirm Teleport",
            Text = "Do you want to teleport to the Dead Rails lobby?",
            Options = {
                {"Confirm", function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/YourRealjohn/Unnamed-HUB-Lua/refs/heads/main/Teleport%20to%20DEAD%20RAILS%20lobby"))()
                end},
                {"Cancel", function()
                    print("Teleport canceled.")
                end}
            }
        })
    end
})

Tab9:AddSection({"Rejoin and Server Hop"})

Tab9:AddButton({
    Name = "Rejoin",
    Callback = function()
        Window:Dialog({
            Title = "Confirm Rejoin",
            Text = "Are you sure do you really want to rejoin?",
            Options = {
                {"Confirm", function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/YourRealjohn/Unnamed-HUB-Lua/refs/heads/main/RejoinServer%20Lua"))()
                end},
                {"Cancel", function()
                    print("Rejoin canceled.")
                end}
            }
        })
    end
})

Tab9:AddButton({
    Name = "Server Hop",
    Callback = function()
        Window:Dialog({
            Title = "Confirm Server Hop",
            Text = "Are you sure you do you want to server hop Huh?",
            Options = {
                {"Confirm", function()
                    local HttpService = game:GetService("HttpService")
                    local TeleportService = game:GetService("TeleportService")
                    local Player = game.Players.LocalPlayer

                    local function serverHop()
                        local gameId = game.PlaceId
                        local servers = {}

                        local success, result = pcall(function()
                            return HttpService:JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..gameId.."/servers/Public?sortOrder=Desc&limit=100"))
                        end)

                        if success and result and result.data then
                            for _, server in ipairs(result.data) do
                                if server.playing < server.maxPlayers and server.id ~= game.JobId then
                                    table.insert(servers, server.id)
                                end
                            end

                            if #servers > 0 then
                                TeleportService:TeleportToPlaceInstance(gameId, servers[math.random(1, #servers)], Player)
                            else
                                warn("No available servers found.")
                            end
                        else
                            warn("Failed to get server list.")
                        end
                    end

                    serverHop()
                end},
                {"Cancel", function()
                    print("Server hop canceled.")
                end}
            }
        })
    end
})